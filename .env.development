# Local development with Electron (`npm run dev`).
# Port defaults: config/dev.json — override below if needed.

# -----------------------------------------------------------------------------
# Supabase
# -----------------------------------------------------------------------------

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=nothing-secret

# -----------------------------------------------------------------------------
# Host and ports (optional overrides)
# -----------------------------------------------------------------------------

# VITE_DEV_SERVER_HOST=127.0.0.1
# VITE_DEV_SERVER_PORT=5173
# ELECTRON_REMOTE_DEBUG_PORT=9223
# ELECTRON_INSPECT_PORT=9229
