import { durationTokens } from '@shared/design-tokens'

/** Vuetify component defaults derived from shared design tokens. */
export const vuetifyDefaults = {
  VTooltip: {
    openDelay: durationTokens.tooltip
  }
}
