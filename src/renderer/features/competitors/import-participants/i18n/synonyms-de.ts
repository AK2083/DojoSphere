/**
 * German Excel header synonyms for participant import auto-mapping.
 *
 * The canonical data lives in the shared domain registry so the Electron main
 * process (column matching) and the renderer (fine-mapping UI) use one source.
 */
export { COMPETITOR_IMPORT_SYNONYMS_DE as default } from '@shared/domain/competitor-import-fields'
